memory sync reconciles the graph with reality at session end or after merges: reads the committed .memory/sync-state.json marker (ancestor check, merge-base fallback, full verification when absent), diffs since the base, reports per-node verdicts (anchors changed / orphaned / fresh), coverage gaps by directory, and prints an agent prompt with a pre-filled save skeleton. Sync never writes graph nodes itself.