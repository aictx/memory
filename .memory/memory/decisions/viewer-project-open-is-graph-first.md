The local viewer's Open project flow should land on the Graph screen. The graph focuses the selected object when one exists; otherwise it picks the highest-degree visible current object with deterministic ID tie-breaking. On small and medium screens, object inspect is reader-first: the selected object hides the list and compact relation overview, shows Back and Graph actions, and places the memory body and direct relations before secondary metadata.