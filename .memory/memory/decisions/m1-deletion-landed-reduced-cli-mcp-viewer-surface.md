M1 of the product-graph rework deleted the dead command surface. Remaining CLI commands: init, check, rebuild, search, inspect, diff, save, remember, reset, projects, view, docs, upgrade. Deleted CLI commands: load, lens, handoff, stale, suggest, patch, setup, audit, graph, export, history, restore, rewind, wiki. Remaining MCP tools: search_memory, inspect_memory, remember_memory (load_memory, save_memory_patch, diff_memory tools deleted; src/mcp/tools/write-queue.ts kept because remember uses it). Deleted modules: src/lenses, src/handoff, src/roles, src/discipline, src/export, src/context (compile/modes/rank/render). src/context/tokens.ts moved to src/core/tokens.ts. ViewerBootstrapData no longer carries role_coverage, lenses, or audit_findings, and the viewer lost its Maintenance and Export screens plus the load-preview surface. maybeStartViewer was salvaged from the deleted setup command into src/cli/commands/view.ts as an exported function for reuse by the future init rework (M5). Test files under test/ still reference deleted modules, so pnpm exec tsc --noEmit reports test-only errors until the M1 test-cleanup follow-up; src/ and viewer/ are green (svelte-check 0 errors 0 warnings).