README documents that Memory is not production-ready yet and breaking changes can occur across package versions while schema and local storage evolve. If a latest-package upgrade later causes errors from existing local Memory storage, run `memory reset` followed by `memory setup`; `memory reset` creates a backup archive under `.memory/.backup/`, and agents should inspect the newest backup to add back only relevant durable project knowledge instead of restoring stale schema files blindly.