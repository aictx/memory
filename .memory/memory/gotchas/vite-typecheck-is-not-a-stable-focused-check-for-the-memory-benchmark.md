For `vite-fs-access-diagnostics`, the broad `pnpm --filter vite typecheck` check is not stable as a focused benchmark check at the pinned Vite commit unless package build output is in the right state. It can fail because ignored `packages/vite/dist` output is absent or partial. The tracked complex Vite task therefore uses the hidden runtime verifier rather than that broad typecheck.